const accesToken
const clientId
const redirectURI

const Spotify = {

    getAccessToken = () => {
        if(accesToken){
            return accesToken
        }
    },
    search = (term) => {

    }
}

export default Spotify
