const token
const clientId
const redirectURI

const Spotify = {

    getAccessToken = () => {
        if(toke){
            return token
        }else{

        }
    },
    search = (term) => {

    }
}

export default Spotify
